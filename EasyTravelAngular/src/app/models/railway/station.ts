import { Time } from '@angular/common';

export class Station {
    code: string;
    station: string;
    stationTrain: string;
    date: string;
    time: Time;
    sortTime: string;
    srcDate: Date;
}